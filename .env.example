# Railway Volume mount path must match this directory.
LIBRARY_RECORDS=/data/records.json

# Use only for the first deployment of an empty Volume. Existing data is never
# overwritten. Change to false after the first successful deployment.
LIBRARY_BOOTSTRAP_SAMPLE=true

# Railway injects PORT automatically. Local example only:
PORT=8000
